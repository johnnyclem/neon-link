## A2 is P4-only. Factory C6 slave is Hosted 1.x — do not pull 2.12.
dependencies:
  espressif/esp_wifi_remote:
    matches:
      - if: "target == esp32p4"
        version: "0.14.*"
  espressif/esp_hosted:
    matches:
      - if: "target == esp32p4"
        version: "1.4.*"

